# Default points at the deployed API so a fresh `bun run dev`
# works without standing up the backend locally. To run against a
# local backend instead, set VITE_API_BASE_URL=http://localhost:3001
# (matches the API repo's default port).
VITE_API_BASE_URL=https://panteon-leaderboard-api.onrender.com
