/// <reference types="cypress" />

/**
 * Custom commands live here. Keep this file thin — every command
 * is a public API surface and changing one ripples through every
 * spec.
 *
 * Example skeleton (kept commented until we need it):
 *
 *   declare global {
 *     namespace Cypress {
 *       interface Chainable {
 *         loginAs(userId: string): Chainable<void>
 *       }
 *     }
 *   }
 *
 *   Cypress.Commands.add('loginAs', (userId) => {
 *     cy.window().then((win) => {
 *       win.localStorage.setItem('panteon.userId', userId)
 *     })
 *   })
 */

export {}
