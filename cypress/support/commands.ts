/// <reference types="cypress" />

/**
 * Custom commands live here. Keep this file thin — every command
 * is a public API surface and changing one ripples through every
 * spec. We currently inline test helpers in the spec rather than
 * elevating them to commands.
 */

export {}
